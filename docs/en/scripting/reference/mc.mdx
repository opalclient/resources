# Minecraft Proxy

**Global Binding:** `mc`

A safe, structured facade over the Minecraft client. It exists so scripts can reach the client without touching intermediary-named fields (GraalVM JS would otherwise see `field_1724` instead of `player` under Fabric intermediary mappings).

<Callout type="info">
    `mc.player` and `mc.world` are exposed for **null checks only**. For actual player/world behaviour, use the dedicated `player` and `world` proxy globals — they return script-safe wrapper types instead of raw intermediary objects.
</Callout>

---

## Client Access

<Method name="getPlayer" signature="LocalPlayer getPlayer()" description="Returns the local player entity, or null if not yet loaded. Intended for null-guard checks only — use the player global for state and movement methods." returns={{ type: "LocalPlayer | null", desc: "Local player, or null." }} />

<Method name="getWorld" signature="ClientLevel getWorld()" description="Returns the active client world, or null if not yet loaded. Intended for null-guard checks only — use the world global for block and entity queries." returns={{ type: "ClientLevel | null", desc: "Client world, or null." }} />

<Method name="getInteractionManager" signature="InteractionManagerProxy getInteractionManager()" description="Returns the interaction manager proxy. Kept for completeness; prefer the public field mc.interactionManager." returns={{ type: "InteractionManagerProxy", desc: "Interaction manager proxy." }} />

<Callout type="info">
    `mc.player`, `mc.world`, and `mc.interactionManager` are also readable as direct fields (GraalVM JS reads them via field access). `mc.player === null` is the idiomatic guard at the top of every event callback.
</Callout>

```javascript
module.on("preGameTick", (event) => {
    // Standard guard — always null-check before doing anything.
    if (mc.player === null || mc.world === null) return;

    client.print("Health: " + player.getHealth());
});
```

---

## Interaction Manager

**Field:** `mc.interactionManager`

Handles block interactions, entity attacks, and block breaking. Every method is null-guarded internally, so it is safe to call before the world or player are ready.

### Block Interaction

<Method name="interactBlock" signature="void interactBlock(InteractionHand hand, HitResult hitResult)" description="Right-clicks a block face with the given hand. The HitResult should come from rotation.getRotationFromRaycastedBlock().getHitResult()." params={[{ name: "hand", type: "InteractionHand", desc: "MAIN_HAND or OFF_HAND." }, { name: "hitResult", type: "HitResult", desc: "Block hit result from a raycasted rotation." }]} />

<Method name="updateBlockBreakingProgress" signature="boolean updateBlockBreakingProgress(BlockPos blockPos, Direction direction)" description="Begins or updates block-breaking progress on the given block position and face." params={[{ name: "blockPos", type: "BlockPos", desc: "Position of the block to break." }, { name: "direction", type: "Direction", desc: "Face being targeted." }]} returns={{ type: "boolean", desc: "True if breaking progress was updated." }} />

<Method name="cancelBlockBreaking" signature="void cancelBlockBreaking()" description="Cancels any in-progress block breaking." />

<Method name="isBreakingBlock" signature="boolean isBreakingBlock()" description="Whether a block break is currently in progress." returns={{ type: "boolean", desc: "True if breaking a block." }} />

### Entity & Item Interaction

<Method name="attackEntity" signature="void attackEntity(Entity entity)" description="Attacks an entity with the local player's main hand." params={[{ name: "entity", type: "Entity", desc: "Entity to attack." }]} />

<Method name="interactItem" signature="void interactItem(InteractionHand hand)" description="Uses the item in the given hand (right-click use, not on a block)." params={[{ name: "hand", type: "InteractionHand", desc: "Hand to use." }]} />

<Method name="stopUsingItem" signature="void stopUsingItem()" description="Stops using an item (e.g. releasing a bow or stopping eating)." />

```javascript
// Place a block using a raycasted hit result, then swing.
mc.interactionManager.interactBlock(MAIN_HAND, raytrace.getHitResult());
player.swingHand(MAIN_HAND);

// Attack the closest entity.
const targets = world.getLivingEntitiesInRange(5.0);
if (!targets.isEmpty()) {
    mc.interactionManager.attackEntity(targets.get(0));
    player.swingHand(MAIN_HAND);
}
```

---

## Related Proxies

`mc` is deliberately thin. The behaviour you reach for lives on these sibling globals, all of which return script-safe wrapper types rather than raw intermediary objects.

| Global | Binding | Use for |
|---|---|---|
| `player` | [Player](#player-proxy) | Position, state, health, combat, entity utilities, swinging |
| `world` | [World](world) | Block queries, entity queries, world info |
| `movement` | [Movement](#movement-proxy) | Speed queries/manipulation, movement direction, input state |
| `rotation` | [Rotation](#rotation-proxy) | Rotation submission, raytraced rotations, rotation math |
| `inventory` | [Inventory](#inventory-proxy) | Slot switching, item searching, stack inspection |

---

## Player Proxy

**Global Binding:** `player`

Local player state, position data, combat utilities, and entity queries.

### Position

<Method name="getEyePosition" signature="Vec3 getEyePosition()" description="Returns the local player's eye position in world space — the origin point for raycasts and rotation calculations." returns={{ type: "Vec3", desc: "Eye position." }} />
<Method name="getPosition" signature="Vec3 getPosition()" description="Returns the local player's eye position in world space." returns={{ type: "Vec3", desc: "Position." }} />
<Method name="getBlockPosition" signature="BlockPos getBlockPosition()" description="Returns the local player's block position (floored coordinates)." returns={{ type: "BlockPos", desc: "Block position." }} />
<Method name="getVelocity" signature="Vec3 getVelocity()" description="Returns the local player's current velocity vector." returns={{ type: "Vec3", desc: "Velocity." }} />
<Method name="getYaw" signature="float getYaw()" description="Returns the local player's current yaw rotation in degrees." returns={{ type: "float", desc: "Yaw." }} />
<Method name="getPitch" signature="float getPitch()" description="Returns the local player's current pitch rotation in degrees." returns={{ type: "float", desc: "Pitch." }} />
<Method name="getFallDistance" signature="double getFallDistance()" description="Returns the local player's current fall distance in blocks." returns={{ type: "double", desc: "Fall distance." }} />

### State

<Method name="isOnGround" signature="boolean isOnGround()" description="Whether the local player is currently on the ground." returns={{ type: "boolean", desc: "True if on ground." }} />
<Method name="isInAir" signature="boolean isInAir()" description="Whether the local player is currently airborne (not on ground)." returns={{ type: "boolean", desc: "True if in air." }} />
<Method name="getAirTicks" signature="int getAirTicks()" description="Number of ticks the player has been in the air." returns={{ type: "int", desc: "Air tick count." }} />
<Method name="getGroundTicks" signature="int getGroundTicks()" description="Number of ticks the player has been on the ground." returns={{ type: "int", desc: "Ground tick count." }} />
<Method name="isSneaking" signature="boolean isSneaking()" description="Whether the local player is currently sneaking." returns={{ type: "boolean", desc: "True if sneaking." }} />
<Method name="isSprinting" signature="boolean isSprinting()" description="Whether the local player is currently sprinting." returns={{ type: "boolean", desc: "True if sprinting." }} />
<Method name="isUsingItem" signature="boolean isUsingItem()" description="Whether the local player is currently using an item (eating, drinking, blocking, drawing a bow, etc.)." returns={{ type: "boolean", desc: "True if using an item." }} />

### Health

<Method name="getHealth" signature="float getHealth()" description="Returns the local player's current health points." returns={{ type: "float", desc: "Health." }} />
<Method name="getMaxHealth" signature="float getMaxHealth()" description="Returns the local player's maximum health points." returns={{ type: "float", desc: "Max health." }} />
<Method name="getAbsorption" signature="float getAbsorption()" description="Returns the local player's current absorption (golden hearts) amount." returns={{ type: "float", desc: "Absorption." }} />

### Combat Utilities

<Method name="canCrit" signature="boolean canCrit()" description="Whether the next hit would be a critical hit (falling, not on ground, not in water, etc.)." returns={{ type: "boolean", desc: "True if a crit is available." }} />
<Method name="getAttackDamage" signature="double getAttackDamage()" description="Attack damage of the item currently in the player's main hand." returns={{ type: "double", desc: "Attack damage." }} />
<Method name="getEntityInteractionRange" signature="double getEntityInteractionRange()" description="Maximum distance at which the player can attack entities." returns={{ type: "double", desc: "Interaction range in blocks." }} />
<Method name="isHoldingWeapon" signature="boolean isHoldingWeapon()" description="Whether the player is holding a weapon (sword, axe, or pickaxe) in their main hand." returns={{ type: "boolean", desc: "True if holding a weapon." }} />

### Entity Utilities

<Method name="getDistanceToEntity" signature="double getDistanceToEntity(Entity entity)" description="Distance from the player to the entity, measured between the closest points of their bounding boxes." params={[{ name: "entity", type: "Entity", desc: "Target entity." }]} returns={{ type: "double", desc: "Distance in blocks." }} />
<Method name="getClosestPoint" signature="Vec3 getClosestPoint(Entity entity)" description="Closest point on an entity's bounding box to the player's eye position (the point KillAura uses for rotation targeting)." params={[{ name: "entity", type: "Entity", desc: "Target entity." }]} returns={{ type: "Vec3", desc: "Closest point." }} />
<Method name="isBoxEmpty" signature="boolean isBoxEmpty(double offsetX, double offsetY, double offsetZ)" description="Whether a bounding box offset from the player's current box is empty (no collidable blocks)." params={[{ name: "offsetX/Y/Z", type: "double", desc: "Offsets to check." }]} returns={{ type: "boolean", desc: "True if empty." }} />
<Method name="isBoxEmptyBelow" signature="boolean isBoxEmptyBelow(double offsetY)" description="Whether the space directly below the player (at the given Y offset) is empty." params={[{ name: "offsetY", type: "double", desc: "Vertical offset (typically negative)." }]} returns={{ type: "boolean", desc: "True if empty." }} />
<Method name="getBoundingBox" signature="AABB getBoundingBox()" description="Returns the bounding box of the local player." returns={{ type: "AABB", desc: "Player bounding box." }} />
<Method name="getStandingEyeHeight" signature="float getStandingEyeHeight()" description="Returns the local player's standing eye height in blocks." returns={{ type: "float", desc: "Eye height." }} />

### Actions

<Method name="swingHand" signature="void swingHand(InteractionHand hand)" description="Plays the hand swing animation for the given hand. Use after placing a block or attacking to trigger the visual swing." params={[{ name: "hand", type: "InteractionHand", desc: "MAIN_HAND or OFF_HAND." }]} />
<Method name="useItem" signature="void useItem(InteractionHand hand)" description="Uses the item in the given hand (right-click) and plays the swing animation." params={[{ name: "hand", type: "InteractionHand", desc: "MAIN_HAND or OFF_HAND." }]} />

```javascript
module.on("preGameTick", (event) => {
    if (player.isOnGround() && player.canCrit()) {
        client.print("Ready to crit!");
    }

    const eyePos = player.getEyePosition();
    client.print("Eye Y: " + eyePos.getY());
});
```

---

## Movement Proxy

**Global Binding:** `movement`

Speed manipulation, direction calculation, and movement state queries.

### Speed Queries

<Method name="getBlocksPerSecond" signature="double getBlocksPerSecond()" description="Current horizontal speed in blocks per second." returns={{ type: "double", desc: "Blocks/second." }} />
<Method name="getSpeed" signature="double getSpeed()" description="Current horizontal speed as a raw velocity magnitude (blocks per tick)." returns={{ type: "double", desc: "Blocks/tick." }} />

### Speed Manipulation

<Method name="yawPos" signature="double[] yawPos(float yaw, double value)" description="Computes the X and Z offsets for a given yaw direction and distance." params={[{ name: "yaw", type: "float", desc: "Direction angle in degrees." }, { name: "value", type: "double", desc: "Distance to project." }]} returns={{ type: "double[]", desc: "[deltaX, deltaZ]." }} />
<Method name="setEntitySpeed" signature="void setEntitySpeed(Entity entity, double speed, double yaw)" description="Sets the velocity of an arbitrary entity along a given yaw direction." params={[{ name: "entity", type: "Entity", desc: "Entity to modify." }, { name: "speed", type: "double", desc: "Target speed in blocks/tick." }, { name: "yaw", type: "double", desc: "Direction angle in degrees." }]} />
<Method name="setSpeed" signature="void setSpeed(double speed)" description="Sets the player's horizontal speed using the current movement input direction." params={[{ name: "speed", type: "double", desc: "Target speed in blocks/tick." }]} />
<Method name="setSpeed" signature="void setSpeed(double speed, double strafePercentage)" description="Sets the player's horizontal speed with a strafe blend between forward and sideways motion." params={[{ name: "speed", type: "double", desc: "Target speed in blocks/tick." }, { name: "strafePercentage", type: "double", desc: "Strafe blend (0.0 = forward, 1.0 = strafe)." }]} />
<Method name="setSpeed" signature="void setSpeed(double speed, float yaw)" description="Sets the player's horizontal speed along a specific yaw direction." params={[{ name: "speed", type: "double", desc: "Target speed in blocks/tick." }, { name: "yaw", type: "float", desc: "Direction angle in degrees." }]} />
<Method name="getSwiftnessSpeed" signature="double getSwiftnessSpeed(double speed, double swiftnessMultiplier)" description="Adjusts a base speed by the player's Swiftness potion effect level." params={[{ name: "speed", type: "double", desc: "Base speed." }, { name: "swiftnessMultiplier", type: "double", desc: "Multiplier per Swiftness level." }]} returns={{ type: "double", desc: "Adjusted speed." }} />
<Method name="getSwiftnessSpeed" signature="double getSwiftnessSpeed(double speed)" description="Adjusts a base speed by the player's Swiftness potion effect level using the default multiplier." params={[{ name: "speed", type: "double", desc: "Base speed." }]} returns={{ type: "double", desc: "Adjusted speed." }} />

### Direction

<Method name="getMoveYaw" signature="float getMoveYaw(Vector2d from, Vector2d to)" description="Calculates the movement yaw between two 2D positions." params={[{ name: "from", type: "Vector2d", desc: "Starting position." }, { name: "to", type: "Vector2d", desc: "Ending position." }]} returns={{ type: "float", desc: "Yaw in degrees." }} />
<Method name="getMoveYaw" signature="float getMoveYaw()" description="Current movement yaw based on WASD input and camera rotation." returns={{ type: "float", desc: "Movement yaw in degrees." }} />
<Method name="getDirectionDegrees" signature="float getDirectionDegrees()" description="Current movement direction in degrees, accounting for strafe and forward input." returns={{ type: "float", desc: "Direction in degrees." }} />
<Method name="getDirectionRadians" signature="double getDirectionRadians()" description="Current movement direction in radians." returns={{ type: "double", desc: "Direction in radians." }} />
<Method name="getDirectionDegrees" signature="float getDirectionDegrees(float yaw)" description="Movement direction in degrees for a specific yaw." params={[{ name: "yaw", type: "float", desc: "Base yaw angle." }]} returns={{ type: "float", desc: "Direction in degrees." }} />
<Method name="getDirectionRadians" signature="double getDirectionRadians(float yaw)" description="Movement direction in radians for a specific yaw." params={[{ name: "yaw", type: "float", desc: "Base yaw angle." }]} returns={{ type: "double", desc: "Direction in radians." }} />
<Method name="getDirection" signature="double getDirection(float rotationYaw, double moveForward, double moveStrafing)" description="Calculates the exact movement direction from raw input values." params={[{ name: "rotationYaw", type: "float", desc: "Player's yaw rotation." }, { name: "moveForward", type: "double", desc: "Forward input (-1 to 1)." }, { name: "moveStrafing", type: "double", desc: "Strafe input (-1 to 1)." }]} returns={{ type: "double", desc: "Direction in radians." }} />

### State

<Method name="isMoving" signature="boolean isMoving()" description="Whether the player is currently providing movement input (any of WASD pressed)." returns={{ type: "boolean", desc: "True if moving." }} />

```javascript
module.on("preGameTick", (event) => {
    if (movement.isMoving()) {
        client.print("Speed: " + movement.getSpeed().toFixed(2));
        movement.setSpeed(0.5);
    }
});
```

---

## Rotation Proxy

**Global Binding:** `rotation`

Both stateless rotation math and stateful rotation submission through the client's rotation handler — the same system KillAura and Scaffold use internally.

<Callout type="info">
    The **stateful** methods (`set`, `setSmooth`) submit a target rotation to the rotation handler, which manages server-side yaw/pitch and movement correction. The **stateless** methods only compute values and never alter client state.
</Callout>

### Rotation Submission (stateful)

<Method name="set" signature="void set(float yaw, float pitch)" description="Submits a target rotation using an instant (snap) model. Applied on the next movement packet with automatic movement correction." params={[{ name: "yaw", type: "float", desc: "Target yaw in degrees." }, { name: "pitch", type: "float", desc: "Target pitch in degrees." }]} />
<Method name="setSmooth" signature="void setSmooth(float yaw, float pitch, float speed)" description="Submits a target rotation using a linear (smooth) model that limits turn speed per tick." params={[{ name: "yaw", type: "float", desc: "Target yaw in degrees." }, { name: "pitch", type: "float", desc: "Target pitch in degrees." }, { name: "speed", type: "float", desc: "Max degrees of rotation per tick." }]} />

### Rotation Calculation (stateless)

<Method name="getRotationFromPosition" signature="Vec2f getRotationFromPosition(Vec3 pos)" description="Yaw and pitch needed to look at a world position from the player's current eye position." params={[{ name: "pos", type: "Vec3", desc: "Target world position." }]} returns={{ type: "Vec2f", desc: "x = yaw, y = pitch." }} />
<Method name="getRotationFromBlock" signature="Vec2f getRotationFromBlock(BlockPos blockPos, Direction direction)" description="Yaw and pitch needed to look at the center of a specific block face." params={[{ name: "blockPos", type: "BlockPos", desc: "Block position." }, { name: "direction", type: "Direction", desc: "Face direction to aim at." }]} returns={{ type: "Vec2f", desc: "x = yaw, y = pitch." }} />
<Method name="getRotationFromRaycastedBlock" signature="RaytracedRotation getRotationFromRaycastedBlock(BlockPos blockPos, Direction side, Vec2f priorityRotations, Vec3 playerPos)" description="Computes a raytraced rotation to a block face, validating that it would hit the intended face within reach. The method Scaffold uses for placement. Returns null if no valid rotation exists." params={[{ name: "blockPos", type: "BlockPos", desc: "Block position to target." }, { name: "side", type: "Direction", desc: "Face to aim at." }, { name: "priorityRotations", type: "Vec2f", desc: "Bias for sorting candidate hit points." }, { name: "playerPos", type: "Vec3", desc: "Eye position to raycast from." }]} returns={{ type: "RaytracedRotation | null", desc: "Rotation + hit result, or null." }} />
<Method name="getRotationFromRaycastedEntity" signature="RaytracedRotation getRotationFromRaycastedEntity(LivingEntity entity, Vec3 closestVector, double entityInteractionRange)" description="Computes a raytraced rotation to a living entity, validating a hit within the given range. The method KillAura uses. Returns null if no valid rotation exists." params={[{ name: "entity", type: "LivingEntity", desc: "Target entity." }, { name: "closestVector", type: "Vec3", desc: "Closest point on the entity's box to the player's eyes." }, { name: "entityInteractionRange", type: "double", desc: "Maximum interaction range." }]} returns={{ type: "RaytracedRotation | null", desc: "Rotation + hit result, or null." }} />
<Method name="getRotationVector" signature="Vec3 getRotationVector(float pitch, float yaw)" description="Returns the unit look vector for the given pitch and yaw." params={[{ name: "pitch", type: "float", desc: "Pitch angle in degrees." }, { name: "yaw", type: "float", desc: "Yaw angle in degrees." }]} returns={{ type: "Vec3", desc: "Unit direction vector." }} />

### Rotation Queries & Math

<Method name="getRotation" signature="Vec2f getRotation()" description="Current server-side rotation as managed by the rotation handler." returns={{ type: "Vec2f", desc: "x = yaw, y = pitch." }} />
<Method name="getRotationDifference" signature="float getRotationDifference(Vec2f a, Vec2f b)" description="Angular difference between two rotations, accounting for wrapping." params={[{ name: "a", type: "Vec2f", desc: "First rotation." }, { name: "b", type: "Vec2f", desc: "Second rotation." }]} returns={{ type: "float", desc: "Total angular difference in degrees." }} />
<Method name="getCursorDelta" signature="double getCursorDelta(double rotationDelta, double sensitivityMultiplier)" description="Raw cursor delta needed to achieve a rotation delta, accounting for Minecraft's sensitivity multiplier." params={[{ name: "rotationDelta", type: "double", desc: "Desired rotation change in degrees." }, { name: "sensitivityMultiplier", type: "double", desc: "Sensitivity scale factor." }]} returns={{ type: "double", desc: "Cursor movement delta." }} />
<Method name="patchConstantRotation" signature="Vec2f patchConstantRotation(Vec2f rotation, Vec2f prevRotation)" description="Patches a rotation to avoid constant-delta detection by applying small natural-looking jitter." params={[{ name: "rotation", type: "Vec2f", desc: "Target rotation." }, { name: "prevRotation", type: "Vec2f", desc: "Previous tick's rotation." }]} returns={{ type: "Vec2f", desc: "Patched rotation." }} />
<Method name="getSensitivityModifiedRotation" signature="float getSensitivityModifiedRotation(double original)" description="Snaps a rotation value to the nearest achievable by Minecraft's mouse sensitivity system." params={[{ name: "original", type: "double", desc: "Desired rotation in degrees." }]} returns={{ type: "float", desc: "Sensitivity-corrected rotation." }} />
<Method name="getSentRotation" signature="Vec2f getSentRotation(Vec2f original)" description="Returns the sent (server-side) rotation after sensitivity correction." params={[{ name: "original", type: "Vec2f", desc: "Original rotation." }]} returns={{ type: "Vec2f", desc: "Sensitivity-modified rotation." }} />
<Method name="getSensitivityModifiedRotationVec" signature="Vec2f getSensitivityModifiedRotationVec(Vec2f original)" description="Applies Minecraft's sensitivity curve to both axes of a rotation." params={[{ name: "original", type: "Vec2f", desc: "Original rotation." }]} returns={{ type: "Vec2f", desc: "Sensitivity-modified rotation." }} />
<Method name="getVanillaRotation" signature="Vec2f getVanillaRotation(Vec2f original)" description="Converts a rotation to vanilla Minecraft mouse-look coordinates." params={[{ name: "original", type: "Vec2f", desc: "Original rotation." }]} returns={{ type: "Vec2f", desc: "Vanilla-equivalent rotation." }} />
<Method name="getDuplicateWrapped" signature="float getDuplicateWrapped(float value, float target)" description="Wraps a value to avoid duplicate-angle detection." params={[{ name: "value", type: "float", desc: "Current rotation value." }, { name: "target", type: "float", desc: "Target rotation value." }]} returns={{ type: "float", desc: "Wrapped value." }} />

### FOV Checks

<Method name="getEntityFOV" signature="double getEntityFOV(Entity entity)" description="Angular offset between the player's look direction and the direction to the entity." params={[{ name: "entity", type: "Entity", desc: "Target entity." }]} returns={{ type: "double", desc: "Angle in degrees." }} />
<Method name="isEntityInFOV" signature="boolean isEntityInFOV(Entity entity, float fov)" description="Whether an entity is within a field-of-view cone centered on the player's look direction." params={[{ name: "entity", type: "Entity", desc: "Entity to check." }, { name: "fov", type: "float", desc: "Half-angle of the cone in degrees (180 = full sphere)." }]} returns={{ type: "boolean", desc: "True if within FOV." }} />

```javascript
module.on("preGameTick", (event) => {
    if (mc.player === null) return;

    const pos = player.getClosestPoint(target);
    const rot = rotation.getRotationFromPosition(pos);
    rotation.setSmooth(rot.getYaw(), rot.getPitch(), 60.0);
});
```

---

## Inventory Proxy

**Global Binding:** `inventory`

Slot switching (with silent/spoof modes), item searching, and stack inspection. Wraps the same slot system Scaffold and other modules use.

### Slot Switching

<Method name="setSlot" signature="void setSlot(int slot)" description="Switches the selected hotbar slot normally (visible to client and server)." params={[{ name: "slot", type: "int", desc: "Hotbar slot index (0-8)." }]} />
<Method name="setSlotSilent" signature="void setSlotSilent(int slot)" description="Switches the selected hotbar slot using the default silent mode — the server receives the change but the held-item render may be preserved." params={[{ name: "slot", type: "int", desc: "Hotbar slot index (0-8)." }]} />
<Method name="setSlotFullSilent" signature="void setSlotFullSilent(int slot)" description="Switches the selected hotbar slot using full silent mode — fully spoofed, the server sees the switch but the client renders the original slot." params={[{ name: "slot", type: "int", desc: "Hotbar slot index (0-8)." }]} />
<Method name="sendSlotPacket" signature="void sendSlotPacket(int slot)" description="Sends a selected-slot update packet to the server for the given slot. Useful to force a switch for a single action within one tick." params={[{ name: "slot", type: "int", desc: "Hotbar slot index (0-8)." }]} />
<Method name="getSelectedSlot" signature="int getSelectedSlot()" description="Returns the currently selected hotbar slot index (0-8)." returns={{ type: "int", desc: "Selected slot index." }} />

### Item Searching

<Method name="findBlock" signature="int findBlock()" description="Searches the hotbar (0-8) for a placeable block, using the same validation as Scaffold. Returns -1 if none found." returns={{ type: "int", desc: "Slot index, or -1." }} />
<Method name="findItem" signature="int findItem(String itemName)" description="Searches the hotbar (0-8) for an item whose display name contains the substring (case-insensitive)." params={[{ name: "itemName", type: "String", desc: "Search string." }]} returns={{ type: "int", desc: "Slot index, or -1." }} />
<Method name="findItemInInventory" signature="int findItemInInventory(String itemName)" description="Searches the full inventory (0-35) for an item by display-name substring (case-insensitive)." params={[{ name: "itemName", type: "String", desc: "Search string." }]} returns={{ type: "int", desc: "Inventory slot index, or -1." }} />

### Stack Inspection

<Method name="getStack" signature="ItemStack getStack(int slot)" description="Returns the item stack in the given hotbar slot." params={[{ name: "slot", type: "int", desc: "Hotbar slot index (0-8)." }]} returns={{ type: "ItemStack", desc: "Item stack." }} />
<Method name="getMainHandStack" signature="ItemStack getMainHandStack()" description="Returns the main hand item stack as resolved by the slot system (accounts for silent switching)." returns={{ type: "ItemStack", desc: "Main hand stack." }} />
<Method name="getOffHandStack" signature="ItemStack getOffHandStack()" description="Returns the off hand item stack." returns={{ type: "ItemStack", desc: "Off hand stack." }} />
<Method name="isHeldItemBlock" signature="boolean isHeldItemBlock()" description="Whether the main hand item is a placeable block." returns={{ type: "boolean", desc: "True if a block." }} />
<Method name="isBlock" signature="boolean isBlock(int slot)" description="Whether the item in the given slot is a placeable block." params={[{ name: "slot", type: "int", desc: "Hotbar slot index (0-8)." }]} returns={{ type: "boolean", desc: "True if a block." }} />
<Method name="getItemName" signature="String getItemName(int slot)" description="Returns the display name of the item in the given slot, or an empty string if the slot is empty." params={[{ name: "slot", type: "int", desc: "Inventory slot index." }]} returns={{ type: "String", desc: "Item name." }} />
<Method name="getItemCount" signature="int getItemCount(int slot)" description="Returns the stack count of the item in the given slot." params={[{ name: "slot", type: "int", desc: "Inventory slot index." }]} returns={{ type: "int", desc: "Item count." }} />
<Method name="countItem" signature="int countItem(String itemName)" description="Counts the total of a specific item across the inventory by display-name substring (case-insensitive)." params={[{ name: "itemName", type: "String", desc: "Item name to search for." }]} returns={{ type: "int", desc: "Total count." }} />
<Method name="countBlocks" signature="int countBlocks()" description="Counts the total placeable blocks in the hotbar (and off hand)." returns={{ type: "int", desc: "Total block count." }} />

```javascript
module.on("preGameTick", (event) => {
    const blockSlot = inventory.findBlock();
    if (blockSlot !== -1) {
        inventory.setSlotSilent(blockSlot);
        client.print("Switched to blocks in slot " + blockSlot);
    }
});
```

---

For the script-safe wrapper types these proxies return (`BlockPos`, `Vec2f`, `Direction`, `RaytracedRotation`, and the hand constants), see [Types](types). For block and entity queries, see [World](world).
